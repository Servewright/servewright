name: servewright_flutter
description: Servewright Flutter renderer engine — semantic SDUI registry, renderer, binding, and transitions.
version: 0.1.0
repository: https://github.com/Servewright/servewright
homepage: https://github.com/Servewright/servewright/tree/main/dart/servewright_flutter

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  servewright_flutter_material:
    path: ../servewright_flutter_material

flutter:
